const AddVideo = () => {
  return (
    <section>
      <form action="">
        <input type="text" />
        <select name="siteType" id="siteType">
          <option value="youtube">YouTube</option>
          <option value="vimeo">Vimeo</option>
        </select>
        <button>Dodaj video</button>
      </form>
    </section>
  );
};

export default AddVideo;
